# 定时任务配置
# 使用方法: crontab crontab.txt

# 每10分钟清理过期房间
*/10 * * * * cd /path/to/your/project && node scripts/cleanup-rooms.js >> logs/cleanup.log 2>&1

# 每小时清理已删除消息（如果需要）
# 0 * * * * cd /path/to/your/project && node scripts/cleanup-messages.js >> logs/cleanup.log 2>&1

# 每天凌晨2点进行数据库备份（示例）
# 0 2 * * * cd /path/to/your/project && node scripts/backup-db.js >> logs/backup.log 2>&1
